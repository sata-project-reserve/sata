name: Prospect Intake Review

on:
  issues:
    types:
      - opened
      - edited
      - reopened
      - labeled
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  review:
    if: github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'prospect-intake')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate prospect intake workflow
        run: npm run ops:prospect-intake-check

      - name: Validate prospect intake commenter
        run: npm run ops:prospect-intake-comment-check

      - name: Prepare prospect review comment
        run: node scripts/prospect-intake-comment-agent.mjs comment-from-event-json "$GITHUB_EVENT_PATH" > .prospect-intake-review-comment.md

      - name: Upsert prospect review comment
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          set -euo pipefail
          existing_id="$(gh api "repos/${GITHUB_REPOSITORY}/issues/${ISSUE_NUMBER}/comments" --jq '.[] | select(.user.login == "github-actions[bot]" and (.body | contains("<!-- sata-prospect-intake-review -->"))) | .id' | tail -n 1)"

          if [ -n "$existing_id" ]; then
            gh api \
              --method PATCH \
              "repos/${GITHUB_REPOSITORY}/issues/comments/${existing_id}" \
              --field body="$(cat .prospect-intake-review-comment.md)"
          else
            gh issue comment "$ISSUE_NUMBER" --body-file .prospect-intake-review-comment.md
          fi
